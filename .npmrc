# Deploy target is Node 24 LTS (see .nvmrc); the toolchain is fully compatible
# with Node >=22.12, so do not hard-fail installs on the engines field.
engine-strict=false
# Keep the dependency graph flat-but-strict: no phantom deps.
shamefully-hoist=false
